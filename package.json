{
  "name": "allegro",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd client && yarn build && cd .. && nodemon server",
    "dev-client": "cd client && cross-env REACT_APP_API_ADDR=localhost:3333 cross-env NODE_API=localhost:3333 yarn start",
    "dev-server": "cross-env NODE_ENV=development PORT=3333 nodemon server --watch server.js",
    "dev": "npm-run-all --parallel dev-server dev-client"
  },
  "dependencies": {
    "@emotion/core": "^10.0.22",
    "@material-ui/core": "^4.8.0",
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "chrome-launcher": "^0.12.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "ramda": "^0.26.1",
    "react-use": "^13.12.2"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "cross-env": "^6.0.3",
    "env-cmd": "^10.0.1",
    "eslint": "^6.8.0",
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5"
  }
}
